spring.application.name=minilinkify-url-api

# Server config
server.port=${PORT:8080}
server.address=0.0.0.0

## H2 database config
#spring.datasource.url=jdbc:h2:mem:minilinkifydb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

## Enable H2 console
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# Postgres Prod (use environment variables) - TEMPORARILY DISABLED FOR TESTING
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/minilinkify}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
# spring.datasource.driver-class-name=org.postgresql.Driver

# H2 database config - TEMPORARILY ENABLED FOR TESTING
spring.datasource.url=jdbc:h2:mem:minilinkifydb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Enable H2 console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

## Postgres Local
#spring.datasource.url=jdbc:postgresql://localhost:5342/minilinkify
#spring.datasource.username=daniyal
#spring.datasource.password=love4all
#spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate config
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Actuator endpoints for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
